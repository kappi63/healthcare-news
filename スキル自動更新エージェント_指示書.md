# スキル自動更新エージェント 指示書（Claude Code ルーティン用）

日次ヘルスケアニュースから、precision-nutrition-analysis スキルへの**追加提案**を作るための手順書。
Claude Code のルーティンにこの指示を渡して使う。**原則: 収集・ドラフトは自動、採用は人。正本は自動で
書き換えない。**

## 前提（リポジトリ構成）
本ルーティンは **Claude Code on the web のスケジュールタスク**として、GitHubリポジトリ上で動く。
ローカルPCのフォルダは使わない。リポジトリに次が配置されていること。

```
<repo>/
├── precision-nutrition-analysis/     … スキル正本（重複チェックに使用。**書き換えない**）
├── precision-nutrition-tests/        … 回帰テスト（現在14件）
├── スキル自動更新エージェント_指示書.md   … 本ファイル
├── proposals/                        … 提案の出力先（YYYY-MM-DD/ を日ごとに作る）
└── （既存のニュース保存先ディレクトリ）
```

- スキル正本を置く目的は**重複チェック**（terms.json・patterns.md・bibliography.md 等に既出でないかの確認）。
- claude.ai でスキルを更新したら、**リポジトリ内の `precision-nutrition-analysis/` も最新版に置き換える**
  （ずれると重複チェックの精度が落ちる）。
- 出力はすべて `proposals/` 配下にコミットする。**正本ディレクトリへのコミットは禁止。**

## ゴール
- その日のニュースから精密栄養学に関係するものを拾い、各候補について「追加提案（差分案＋理由＋出典）」を
  ステージングに出力する。マージはしない。

## 手順
1. **収集**: その日のヘルスケアニュースを取得（既存ルーティンの収集結果をそのまま入力にする）。
2. **一次情報の確認**: 見出し/記事だけで判断せず、元の出典（学会ガイドライン・査読論文・公的機関の発表）を
   たどって内容とエビデンスの強さを確認する。たどれない/出所不明は**却下**。
3. **関連フィルタ**: スキルが扱うマーカー・栄養素・パターン（血液マーカー、ビタミン/ミネラル、糖代謝・
   脂質・鉄・甲状腺・腸内・遺伝子・エピジェネティクス等）に関係するものだけ残す。無関係は却下。
4. **重複チェック**: `references/terms.json`・`patterns.md`・`bibliography.md`・各 functional-tests を検索し、
   既出でないか、既存の更新（上書き）かを判定する。
5. **評価と提案作成**: 残った各候補に、下の「提案テンプレート」で8項目の理由と差分案を作る。
6. **出力**: 提案を `proposals/YYYY-MM-DD/` に Markdown で保存し、**コミットする**（1候補1ファイル）。
   **正本ファイル（precision-nutrition-analysis/ 配下）は直接編集しない。**
   その日の要約を `proposals/YYYY-MM-DD/README.md` に置くと後から追いやすい。
7. **追跡リスト（watchlist.md）との突き合わせ**: `references/watchlist.md` の T1〜T6 に該当する話題なら、
   提案を作るか、**watchlist の該当行の「次のチェックポイント」を更新する案**を出す（正本は書き換えない）。
   特に T1（NIH NPH・2027年データ公開）と T4（食事摂取基準2030）は理想値に関わるため、
   **記録に留め、変更提案はしない**。
8. **通知**: その日の提案一覧（採用推奨/保留/見送りの別と要約）を人に渡す。該当なしの日は「該当なし」と一行で報告。

## 却下・保留の基準（重要）
- 出典が reputable でない（まとめサイト・出所不明・広告）→ 却下。
- 単発の小規模観察研究・プレスリリースのみ → **保留**（"確立"にしない。数が揃うまで寝かせる）。
- カウンセリングの助言に落ちない（概念のみ）→ 見送り、または「枠組み」提案に留める。
- **理想値（optimal/reference）の変更を含む** → **日次ニュース由来では変更しない（禁止）**。理想値は
  神宮前クリニックの体系に基づく土台で、速報では動かさない。関連ニュースは「参考情報」として提案・記録に
  留め、変更が要ると思われる場合も「要・施術者判断」とだけ書いて提案止まりにする。自動反映は絶対にしない。
- 疾患の診断・治療に踏み込む内容 → 栄養の範囲に限定し、医師案件フラグを立てる。
- **既存の禁止表現・安全ルールと矛盾する内容** → 却下または要注意フラグ。既存ルールの例:
  ビタミンDの感染予防を謳わない／「サブグループでは有効性が確認」と書かない／「やせ菌・デブ菌」を使わない／
  遺伝子の決定論的表現を使わない／薬の変更・中止を提案しない／毛髪検査でキレーションを勧めない。
  （詳細は `references/output-checklist.md` と各モジュール）

## エビデンスタグ（既存ルールに合わせる）
確立／中程度／仮説／疾患関連_要医師。ガイドライン・メタ解析・大規模RCT＝確立寄り、小規模・観察・
機序のみ＝中〜仮説。断定を避け「〜とされる」の語調。

---

## 提案テンプレート（この形で1候補1ファイル）

```
# 追加提案: 〈トピック名〉  （日付 / 記者AI）

## 1. 関連性
〈なぜこのスキルに関係するか。触れるマーカー/パターン/栄養素〉

## 2. 新規性（重複チェック結果）
〈terms.json/patterns.md/bibliography のどこに既出か。新規 or 既存更新 or 既出（→見送り）〉

## 3. エビデンスの強さ
情報源: 〈ガイドライン/メタ解析/RCT/観察/専門家/報道〉
タグ: 〈確立 / 中程度 / 仮説 / 疾患関連_要医師〉
一言評価: 〈なぜそのタグか〉

## 4. 実行可能性
〈カウンセリング助言に落ちるか。食材・剤形・量・タイミング・注意。落ちないなら「見送り/枠組みのみ」〉

## 5. 反映先（差分案）
対象ファイル: 〈例: patterns.md D2 / terms.json vitamin-d〉
差分案:
〈追記/修正する具体文。※理想値(optimal/reference)は日次ニュース由来では変更しない。関連する場合も据え置きとし、参考情報として本文に注記するに留める〉

## 6. 安全・医療フラグ
〈疾患・禁忌・過剰リスク・妊娠/小児など。医師案件なら明記。なければ「特になし」〉

## 7. 出典（bibliography 追加行）
| S0XX | 〈書誌・著者・誌名・年〉 | 〈トピック〉 | 〈反映先〉 | 〈タグ〉 | 〈日付〉 |
URL/DOI: 〈…〉

## 8. 回帰テスト
影響しうる case: 〈A/B/C/なし〉
確認内容: 〈例: caseBのビタミンD欠乏検出が維持されるか〉

## 推奨
〈採用 / 保留 / 見送り〉 — 理由: 〈一言〉
```

---

## お手本（先日のビタミンD更新がこの理想形）
- 実行可能な知見（D3優先・高用量の非有益性・Ca併用の結石リスク）
- 出典が一流誌のRCT/メタ解析（Tripkovic 2012, Balachandar 2021, Burt 2019, WHI 2006/2011）＝【確立】
- 反映先が明確（patterns.md D2 / terms.json vitamin-d）、**理想値は据え置き**
- 回帰テスト caseB/C に影響なし（検出ルール不変）
ニュース由来の提案も、この粒度で上がってくるのが目標。

## 承認後（人が実行、またはClaude Codeに指示）
1. 提案の差分を正本に反映。**理想値（optimal/reference）は触らない。**
2. 検証: skill-creator の `quick_validate` を通す。
3. **回帰テスト（現在15件）** を走らせ、各ケースの「必ず拾う所見」「やってはいけない誤り」を確認。
   基本3件（A/B/C）は最低限必須。変更領域に関係するケースは必ず含める
   （例: ビタミンD関連→V1-V5、血糖→H1/H2、相互作用→I1、ライフステージ→S1、脂質骨→J1、経時比較→L1）。
4. `bibliography.md` に出典1行（S0XX）、`UPDATING.md` の変更履歴に1行（レビュー=反映済）。
5. スキルを再パッケージ（package_skill）し、`.skill`／`.zip` を更新。
6. claude.ai 側で「スキルを更新」。**リポジトリ内の `precision-nutrition-analysis/` も最新版に置き換えてコミット**。

## 別ルート（推奨・手軽）
提案をそのまま Claude（claude.ai）のチャットに貼り、「これをスキルに反映して」と依頼してもよい。
反映・検証・回帰確認・再パッケージまで行い、更新ボタン付きで返る。先日のビタミンD更新がこの方式。

