# GitHub＋スケジュールタスク 設定手順

Claude Code on the web のスケジュールタスクで、日次ニュースから
精密栄養スキルへの「追加提案」を自動生成するための設定手順です。

---

## ステップ1: リポジトリにファイルを追加する

ニュースを保存している既存のGitHubリポジトリに、この一式（4点）を追加します。

```
precision-nutrition-analysis/     ← スキル正本（重複チェック用）
precision-nutrition-tests/        ← 回帰テスト（14件）
proposals/                        ← 提案の出力先
スキル自動更新エージェント_指示書.md
```

### 方法A: GitHubのWeb画面から（かんたん・推奨）
1. リポジトリのページを開く
2. 「Add file」→「Upload files」をクリック
3. この一式のフォルダごとドラッグ＆ドロップ
4. 下の「Commit changes」で `スキル自動更新の一式を追加` などと入力してコミット

> フォルダごとドラッグすれば階層のままアップロードされます。
> `proposals/` は空だとアップロードできないため、`.gitkeep` と `README.md` を同梱しています。

### 方法B: Claude Code に任せる（もっとかんたん）
この一式をアップロードしたうえで、Claude Code のセッションで次のように依頼します。

> 「アップロードした precision-nutrition-analysis / precision-nutrition-tests /
> proposals / スキル自動更新エージェント_指示書.md をリポジトリのルートに追加してコミットして」

### 方法C: 手元のPCから（gitを使い慣れている場合）
```bash
cd <リポジトリのフォルダ>
# ダウンロードした一式をここに展開してから
git add precision-nutrition-analysis precision-nutrition-tests proposals "スキル自動更新エージェント_指示書.md"
git commit -m "スキル自動更新の一式を追加（正本・回帰テスト・提案出力先・指示書）"
git push
```

---

## ステップ2: スケジュールタスクのプロンプトを編集する

1. `claude.ai/code` を開く
2. ニュース収集のセッションを選ぶ
3. **スケジュール設定**を開く
4. 今のプロンプト（手順書）の**末尾に、下の追記ブロックをそのまま貼る**
5. 保存

### 追記ブロック（そのままコピーして貼り付け）

```
---

## 追加タスク: 精密栄養スキルへの提案作成

上記のニュース収集とGitHub保存が完了したら、続けて以下を実行すること。

1. リポジトリ内の「スキル自動更新エージェント_指示書.md」を読み、その手順に従う。
2. 本日収集したニュースの中から、精密栄養学（血液マーカー、ビタミン・ミネラル、
   糖代謝、脂質、鉄、甲状腺、腸内環境、遺伝子・エピジェネティクス等）に
   関係するものを抽出する。
3. 各候補について、指示書の「提案テンプレート」に沿って8項目の理由を付けた
   追加提案を作成し、proposals/YYYY-MM-DD/ に1候補1ファイルで保存する。
   その日の要約を proposals/YYYY-MM-DD/README.md にも記載する。
4. 該当するニュースがなければ、proposals/YYYY-MM-DD/README.md に
   「本日は該当なし」とだけ記録する。
5. 変更をコミットする。

### 厳守事項
- precision-nutrition-analysis/ 配下（スキル正本）は絶対に書き換えないこと。
  このタスクの役割は「提案の作成」までであり、採用の判断は人が行う。
- 理想値（optimal / reference）の変更は提案しないこと。
  日次ニュース由来では理想値を変更しないルールになっている。
- 出典が確認できないもの（出所不明のまとめサイト等）は採用しないこと。
  必ず一次情報（学会ガイドライン・査読論文・公的機関）をたどって評価する。
- 提案には必ずエビデンスタグ（確立／中程度／仮説／疾患関連_要医師）を付けること。
- 既存の禁止表現と矛盾する内容は却下すること
  （例: ビタミンDの感染予防を謳う、やせ菌・デブ菌、遺伝子の決定論的表現、薬の変更提案）。
```

---

## ステップ3: 動作を確認する

翌朝（または手動実行して）、リポジトリの `proposals/` に日付フォルダができているか確認します。

- **提案が出ている** → 中身の8項目を読み、採用したいものを判断
- **「本日は該当なし」** → 正常です。毎日提案が出るわけではありません

最初の数日は、提案の粒度（多すぎる／少なすぎる、拾う範囲が広すぎる等）を見て、
スケジュール設定のプロンプトを微調整してください。

---

## 日々の運用フロー

1. 朝、`proposals/` を確認する
2. 採用したい提案があれば、**その内容を claude.ai のチャットに貼り**「これをスキルに反映して」と依頼
3. 反映・検証・回帰テスト・再パッケージが行われ、更新用の `.skill` が返る
4. 「スキルを更新」ボタンで反映する
5. **リポジトリ内の `precision-nutrition-analysis/` も最新版に置き換えてコミット**
   （ここがずれると重複チェックの精度が落ちるため、忘れずに）

---

## 困ったときは

- **提案が毎日大量に出る** → プロンプトに「1日あたり最大3件まで、重要度の高いものに絞る」を追記
- **関係ないものが混ざる** → 「血液マーカーまたは栄養素に直接関係するものに限定する」を追記
- **提案が出ない日が続く** → 正常な可能性が高い（精密栄養に関わる確かな新知見は毎日は出ない）
- **正本が書き換えられていた** → 厳守事項が効いていないので、プロンプトの該当部分を強調して再保存
